# ⚠️ ISOLATED TEST ENVIRONMENT - NEVER CONNECTS TO PRODUCTION ⚠️
# This file is ONLY for testing and is completely separate from production
# CI will override DATABASE_URL with its own isolated PostgreSQL database
DATABASE_URL="file:./test_isolated_local.db"
NODE_ENV=test
NEXTAUTH_SECRET=test-secret-isolated-never-production
NEXTAUTH_URL=http://localhost:3000

# This ensures tests NEVER accidentally connect to production
PRODUCTION_GUARD=isolated_test_environment